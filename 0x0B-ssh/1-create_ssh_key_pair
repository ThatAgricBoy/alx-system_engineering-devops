#!/bin/env bash

# Key pair information
school="private_key_file"
key_bits=4096
betty="passphrase"
# Generate RSA key pair
ssh-keygen -t rsa -b "$key_bits" -C "samueljohn3999@gmail.com" -f "$school" -N "$betty"

echo "RSA key pair created successfully."
